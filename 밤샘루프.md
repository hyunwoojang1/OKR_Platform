# 밤샘루프.md — 목표관리설정 허브 무인 구축 로그

> 시작: 2026-08-17 23:10 KST · 운영: 반복 사이 2분 휴식 · 기록 원칙: 라운드마다 「한 것 / 결과 / 발견·판정 / 다음」
> 최종 목표: 목표 달성 툴 + econ·경매·job 크롤러가 읽기/쓰기로 이어지는 단일 허브
> 안전 원칙: ① Supabase DDL은 `goalhub` 스키마 한정 ② econ·auction 기존 테이블은 읽기 전용 ③ 기존 사이트 인증은 플래그 OFF로만 배포 ④ 파괴적 작업(기존 프로덕션 동작 변경) 금지

## 아침에 현우가 할 일 (사용자 체크리스트)

- [ ] **Google OAuth 발급** (10분, hyunwoojang99@gmail.com): ① console.cloud.google.com → 새 프로젝트 "goal-hub" ② OAuth 동의화면(External, 테스트 사용자에 본인 추가) ③ Google Calendar API 사용 설정 ④ 사용자 인증 정보 → OAuth 클라이언트 ID(웹) 생성 — **승인된 리디렉션 URI = `https://goal-hub-blue.vercel.app/api/auth/callback` 그리고 `https://econ-dashboard-one.vercel.app/api/auth/callback`(econ 실제 도메인으로 확인 후) + `http://localhost:3800/api/auth/callback`** ⑤ ID/Secret을 Claude에게 전달
- [ ] econ-dashboard 로그인 스위치 켜기 (Claude와 함께, 폰 옆에서)
- [ ] 폰 홈화면에 새 앱 추가 + 알림 권한 허용

## 라운드 로그

### R0 — 계획 수립 (23:04~23:10)
- 그릴 2차 완료(N1~N6), DECISIONS.md·versions.md 갱신, 이 파일 생성
- 라운드 계획: R1 스캐폴드 → R2 goalhub 스키마 → R3 인증뼈대(우회세션+OAuth 코드) → R4 CRUD → R5 벤토 '오늘' → R6 푸시 → R7 브리핑엔진+크론 → R8 마감루프 → R9 허브 읽기타일 3종(+job 브리지) → R10 쓰기 2종 → R11 econ 로그인 플래그OFF → R12 배포 → R13~ E2E 검수루프(webapp-testing+감사판정)
- 레퍼런스 리서치는 R4(OKR UI)·R5(벤토)·R6(푸시 UX) 직전에 각각 수행

### R1 — 스캐폴드 (23:10~23:16)
- 한 것: git init(main) + create-next-app(TS·Tailwind·App Router·src-dir) → `app/`, 루트 .gitignore, git 신원 설정(hyunwoojang1)
- 결과: 커밋 cbb295a

### R2 — goalhub 스키마 (23:12~23:16, R1과 병행)
- 한 것: `db/001_goalhub_schema.sql` 작성 → Supabase Management API로 적용 (기존 프로젝트 trajmfklbyarbkiljogj, goalhub 스키마 한정)
- 결과: 13개 테이블 생성 검증 완료, **RLS 전부 ON + 정책 0개 = anon/authenticated 완전 차단(fail-closed), service_role만 통과**. econ·auction 스키마는 안 건드림
- 다음: R3 인증 뼈대 (개발용 우회 세션 + Google OAuth 코드, AUTH_MODE 플래그)

### R3 — 인증 뼈대 (23:24~23:34)
- 한 것: HMAC-SHA256 서명 세션쿠키(session.ts, Edge/Node 겸용) + proxy 게이트(공개경로 화이트리스트, SESSION_SECRET 없으면 503 fail-closed) + AUTH_MODE 이중모드(dev=우회세션 즉석발급 / google=실 OAuth) + Google OAuth login/callback/logout 라우트(state 검증, 이메일 화이트리스트, email_verified 확인, refresh_token 저장용 oauth_tokens 표) + /login 화면 + goalhub 스키마 PostgREST 노출(public 유지, 추가만) + app/.env.local(Supabase 키 복사, SESSION_SECRET 생성)
- 결과: tsc 0에러·빌드 성공, 커밋 ffb64e2. Next 16 middleware→proxy 규약 이전 완료
- 발견: create-next-app이 Next 16을 깔았고 middleware가 deprecated → proxy.ts로 선제 전환 (E2E 때 게이트 동작 재확인 예정)
- 다음: R4 CRUD (레퍼런스 리서치 → 영역/OKR/습관/할일 API+화면)

### R4 — 레퍼런스 리서치 + CRUD (23:30~23:52)
- 리서치(research-analyst): Sunsama 아침 리추얼(카드 승인/보류)·워크로드 경고, Streaks 잔디 히트맵, Habitify 진행률 링, Weekdone 4색 신호등+트리 롤업, Perdoo 브레드크럼, 벤토 "한 타일=한 지표" 원칙 채택. 권장 타일 7종 확보 → R5·R7에 반영
- 한 것: types.ts(KST 날짜 유틸 포함)·actions.ts(서버액션 15종, 입력검증·에러 명시)·queries.ts(트리 조회+기본영역 6종 시드+스트릭/주간카운트) + /okr 트리 화면(신호등 진척바·인라인 KR 갱신·마일스톤/이니셔티브 인라인 추가) + /habits(오늘 원탭 체크·28일 잔디·주간 링) + 디자인 토큰(globals.css 라이트/다크)·하단 탭 네비
- 발견→수정: **service_role에 신규 스키마 권한이 자동 부여 안 됨**(500 permission denied) → 003_service_role_grants.sql로 usage+all+default privileges 부여. anon 차단은 유지
- 결과: 빌드 초록, 실서버 스모크 /okr·/habits 200 + 기본영역 시드 확인. 커밋
- 다음: R5 벤토 '오늘' 화면 + /calendar + PWA manifest

### R5 — 벤토 '오늘' + 캘린더 + PWA 껍데기 (23:38~23:50)
- 한 것: `/`(벤토: 오늘할일 큰타일[이월배지·완료접기·영역점]+오늘의습관 원탭+오늘일정+이번주 이니셔티브 칩[↴ 오늘할일로 내리기]) + `/calendar`(14일 아젠다·추가/삭제·Google 동기화 대기 표시) + manifest.webmanifest(standalone·ko) + sw.js(push 수신·notificationclick 이동) + 아이콘 192/512(과녁 모티프, PIL 생성) + SW 등록 스크립트
- 결과: 빌드 초록, 서버 재기동 후 5개 경로 전부 200. 커밋
- 다음: R6 푸시 파이프라인 (VAPID 생성 → 구독 API → 발송 유틸 → 알림 설정 UI)

### R6 — 푸시 파이프라인 (23:43~23:55)
- 한 것: VAPID 키 생성(.env.local), push.ts 발송 유틸(410/404 만료구독 자동 비활성·last_success 기록), /api/push/subscribe GET(공개키)/POST(upsert 저장, endpoint·keys 검증)/DELETE, /api/push/test, /settings 화면(알림 켜기/끄기/테스트 클라이언트 컴포넌트 — iOS 홈화면 안내 포함), 네비에 설정 추가
- 결과: 빌드 초록, 공개키 API·테스트 발송(sent 0=구독 전 정상)·/settings 200. 커밋
- 다음: R7 브리핑 엔진(규칙: 마감임박>이월>진척뒤처짐) + 크론 라우트(morning/evening, CRON_SECRET 게이트) + 이월 처리

### R7 — 브리핑 엔진 + 크론 (23:47~23:59)
- 한 것: briefing.ts(자동이월→규칙점수[마감 100/60/30 + 이월×12 + 이니셔티브 8]→푸시 전체목록 조립, 분기 경과율-진척률>15%p KR 경고 상위3, 저녁 잔여집계), cron-guard(CRON_SECRET 미설정=차단), /api/cron/morning-briefing·evening-reminder(브리핑 briefings 표 기록→푸시), vercel.json 크론 2개(UTC 22:00=KST 아침7시, UTC 12:00=KST 밤9시), CRON_SECRET 생성
- 결과: 시크릿 없이 401·시크릿으로 아침/저녁 크론 실행 성공(브리핑 기록+발송 시도). 커밋
- 다음: R8 마감 화면 /close (10초 체크 + 한 줄 회고)

### R8 — 마감 화면 (23:51~23:57)
- 한 것: /close — 오늘 요약 밴드(할일 n/m·습관 n/m), 남은 할일·습관을 큰 탭 버튼으로 10초 마감(미체크는 「내일 자동 이월」 안내), 한 줄 회고 입력(daily_reviews upsert, 저장됨 표시)
- 결과: 빌드 초록, /close 200. 커밋
- 다음: R9 허브 읽기 타일 — econ·auction 실물 스키마 탐색(읽기 전용) → 타일 3종 + job 크롤러 브리지

### R9 — 데이터 허브 (23:55~00:12)
- 탐색: public 스키마 21표 확인. 핵심 소스 = auction_scored_listings(36컬럼, 보수차익 profit_low)·daily_reports·news_items·**public.job_postings(econ 채용탭이 이미 매일 적재 중!)**·portfolio_holdings
- 한 것: hub-sources.ts(★SELECT만 원칙 주석 명시) — 경매 arb_score 상위(매각기일 미래만)·econ 최신리포트+중요뉴스3·채용(goalhub 브리지+econ 합본, url 중복제거) / /hub 3타일(실패 시 타일별 격리 에러표시 Promise.allSettled) / 쓰기1호 sendJobToTask(공고→[지원검토] 취업 할일, goalhub에만 씀) / bridge/upload_jobs.py(범용 JSON 업로더, Content-Profile: goalhub — **크롤러 레포 연결은 내일 감독 하에**) / 네비 재편(허브 탭 추가, 설정은 오늘화면 ⚙️로)
- 결과: /hub 200, 3타일 렌더, 채용 실데이터(자산운용사 공고들) 유입, 불러오기 실패 0. 커밋
- 판정 메모: job_applications 크롤러의 일자 폴더 구조가 예상과 달라 직접 파싱 대신 범용 브리지로 우회 — 크롤러 수정은 야간 금지 원칙에 부합
- 다음: R10 쓰기 2호 — KR 자동채움(habit_agg 집계 크론 반영) + api 커넥터 자리 검증

### R10 — KR 자동채움 + 중대버그 수정 (00:01~00:26)
- 한 것: kr-sync.ts(habit_agg=분기 내 습관로그 집계 / api 커넥터 2종: auction 양호등급 수·jobs_sent) → 아침 크론 통합(실패해도 브리핑 계속), KR 생성 폼에 자동연결 select(습관 목록+커넥터), /okr에 ↻수동갱신
- 🔴 발견 1(CRITICAL, 수정완료): **areas 시드 폭주** — seed-on-read 재귀가 6종을 1,973회 삽입(11,838행). 004 마이그레이션으로 FK 재지정→중복삭제→**unique(name)**, 코드는 재귀 제거+ignoreDuplicates upsert로 멱등화. 검증: 6행 복구
- 🟡 발견 2(수정완료): 카운터 맹점 — 값 무변화 시 updated/skipped 어느 쪽도 안 올라 「미처리」로 오독됨 → unchanged 카운터 추가
- 🟡 발견 3(수정완료): 경매 등급은 S/A가 아니라 한글 라벨('양호' 75·'관심' 174…) — 커넥터를 '양호' 기준으로 교정
- 🟡 발견 4(예방적용): git-bash 인라인 curl의 한글이 CP949로 깨짐 → SQL 페이로드는 python UTF-8 파일 경유 원칙. supabase fetch에 no-store 명시(Next 캐시 예방)
- 결과: **E2E 관통 — 크론 실행 시 경매 양호 75건이 KR current_value에 자동 반영(0→75), updated=1**. 테스트 데이터 정리 완료. 커밋
- 다음: R11 econ-dashboard Google 로그인 코드+AUTH_REQUIRED=false (커밋만, 스위치 금지)

### R11 — econ-dashboard 보안 개조 (00:12~00:16)
- 사전: git fetch → origin/main 동기 확인(다중세션 규칙), 기존 미들웨어 없음 확인, 타세션 untracked(ARCHITECTURE.md)는 커밋에서 제외(명시적 add만)
- 한 것: src/proxy.ts(AUTH_REQUIRED 플래그 게이트 — **기본 OFF=무동작**, ON이어도 /api/**는 통과라 매시 크론·GitHub Actions 무영향, 락아웃 복구=env 제거) + auth-session.ts(HMAC 쿠키) + /api/auth 3종(화이트리스트 fail-closed, scope=openid email만) + /login. versions.md v0.300.0 기재
- 결과: tsc 0에러, econ 로컬 커밋 9b32cff (**푸시·배포·스위치 안 함** — 내일 감독 하에). 켜기 전 필요 env: GOOGLE_CLIENT_ID/SECRET·AUTH_SESSION_SECRET·AUTH_REQUIRED=true
- 다음: R12 목표허브 Vercel 배포 (새 프로젝트 goal-hub, env 6종, 프로덕션)

### R12 — Vercel 프로덕션 배포 (00:17~00:29)
- 배포 전 보안 보강: dev 우회 세션에 **DEV_GATE_TOKEN 임시 자물쇠**(?devkey= 1회 입력→쿠키, 없으면 401) — 공개 URL에서 아무나 들어오는 구멍 봉쇄. Google 전환 시 자연 소멸
- 한 것: vercel link(신규 프로젝트 goal-hub, hyunwoo-jang-s-projects 스코프) → 프로덕션 env 10종 주입 → --prod 직배포(37s Ready). vercel.json 크론 2개 등록됨
- ⚠️ 발견: goal-hub.vercel.app은 **타인 프로젝트가 선점한 도메인**(Vite 앱이 뜸) — 우리 정식 도메인은 **https://goal-hub-blue.vercel.app**. 혼동 금지!
- 실사 결과: 무키 401 잠금 ✓ / devkey→200+오늘 타일 ✓ / 쿠키 유지 /hub 3타일 실데이터 ✓ / 크론 무시크릿 401·시크릿 실행 OK ✓
- 다음: R13+ webapp-testing E2E 실유저 주행 + 감사식 적대판정 루프

### R13 — E2E 실유저 주행 + 적대판정 (00:24~00:48)
- 주행: Playwright(폰 뷰포트 390×844) 7시나리오 14검증 — 할일 추가/체크·분기목표·자동KR 생성/갱신·습관 생성/체크·일정 추가/오늘 반영·마감 체크/회고·공고→할일·설정
- 1차 6/12 → 적대판정: **전부 앱 버그 아님을 DB로 증명**(모든 쓰기·sync 75 반영 확인). 진범 2건은 하네스 결함:
  - 🔴 판정A(수정): **서버 떠 있는 채로 .next 재빌드**(R12) → 라이브 서버가 혼합 빌드 서빙 → stale 렌더. 교훈: 빌드 후 반드시 재기동. 재기동 후 8/12
  - 🟡 판정B(수정): 클릭 직후 networkidle이 POST 시작 전에 통과 → goto가 서버액션과 경주(플레이키). 고정대기 1.8s로 교체 → **14/14 PASS**
- 정리: E2E 데이터 전량 삭제(DB 카운트 0 확인), 스크립트는 tests/e2e_goalhub.py로 편입
- MEDIUM 기록(수정 안 함, 아침 보고): ①서버액션 실패 시 사용자용 에러 UI 없음(Next 기본 에러화면) ②허브 공고 「할일로」는 econ 공고를 goalhub에 사본 upsert — 원본 크롤 갱신과 중복 가능성 낮지만 url unique로 방어됨 ③iOS 푸시는 홈화면 추가 후에만(설정 화면에 안내 있음)
- 다음: R14 프로덕션 최종 스모크 + NIGHT_REPORT + 사용자 체크리스트 → stop

### R14 — 마무리 (00:34~00:45)
- 발견→해결: 프로덕션 devkey 검증 중 **Vercel sensitive env는 pull로 값 복구 불가** 확인(빈 문자열 반환) + 로컬 사본은 이미 삭제 → **토큰 회전**(신규 생성→env --force→재배포)
- 최종 스모크: 무키 401 ✓ / devkey 200+오늘 타일 ✓ / 7페이지 전부 200 ✓ / 크론 게이트 ✓
- NIGHT_REPORT.md 작성(5단 브리핑+사용자 체크리스트 4건+MEDIUM 목록), versions.md 갱신, 메모리 갱신
- **루프 종료.** 총 R0~R14, 커밋 12개, E2E 14/14, 앱 버그 0(하네스 결함 2건 수정)
